using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WasteScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        this.name = "Waste";
        this.tag = "ObjectsToBePlaced";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
